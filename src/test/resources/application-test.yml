spring:
  jpa:
    hibernate:
      ddl-auto: create-drop

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test-client-id
            client-secret: test-client-secret
            scope:
              - openid

jwt:
  secret-key: test-secret-key-for-hs256-token-signing
  access-expiration-ms: 1800000
  refresh-expiration-ms: 604800000
